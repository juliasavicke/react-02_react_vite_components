import SectionTitle from './SectionTitle';
import WorkGrid from './WorkGrid';

function OurWork() {
  return (
    <div className='workSection container'>
      <SectionTitle title='Work section' />
      <WorkGrid />
    </div>
  );
}
export default OurWork;
